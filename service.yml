apiVersion: v1
kind: Service
metadata:
  name: flask-container-app-service
spec:
  type: NodePort
  selector:
    app: flask-container-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
